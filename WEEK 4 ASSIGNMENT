def modify_content(content):
    """
    Function to modify file content.
    For this example, we'll just convert text to uppercase.
    You can customize this function.
    """
    return content.upper()

# Ask user for filename
filename = input("Enter the filename to read: ")

try:
    # Try to open and read file
    with open(filename, "r") as infile:
        content = infile.read()

    # Modify content
    modified_content = modify_content(content)

    # Write to a new file
    new_filename = "modified_" + filename
    with open(new_filename, "w") as outfile:
        outfile.write(modified_content)

    print(f"File modified successfully! New file saved as: {new_filename}")

except FileNotFoundError:
    print("Error: The file does not exist. Please check the filename.")
except PermissionError:
    print("Error: You don’t have permission to read this file.")
except Exception as e:
    print(f"An unexpected error occurred: {e}")


Enter the filename to read: notes.txt
File modified successfully! New file saved as: modified_notes.txt


Enter the filename to read: missing.txt
Error: The file does not exist. Please check the filename.
